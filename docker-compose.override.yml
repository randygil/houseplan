# Local only (auto-loaded by `docker compose`; Coolify ignores it). Prod = docker-compose.yml alone.
services:
  db:
    ports: ["127.0.0.1:${DB_PORT:-5433}:5432"]
  api:
    ports: ["127.0.0.1:3000:3000"]
  web:
    ports: ["8080:80"]
